extends layout

block content
  div
    a(href="/") 予定一覧に戻る
  form(action="/schedules", method="post")
    div
      h5 予定名
      input(type="text", name="scheduleName")
    div
      h5 メモ
      textarea(name="memo")
    div
      h5 候補日程（改行して複数入力してください）
      textarea(name="candidates")
    button(type="submit") 予定を作成